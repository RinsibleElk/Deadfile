<UserControl x:Class="Deadfile.Tab.Quotes.QuotesBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Deadfile.Tab.Quotes"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=local:QuotesBarDesignTimeViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock Style="{StaticResource QuotationPhrase}" Grid.Row="0" Grid.Column="0" Text="{Binding Quotation.Phrase}"></TextBlock>
        <TextBlock Style="{StaticResource QuotationAuthor}" Grid.Row="1" Grid.Column="0" Text="{Binding Quotation.Author}"></TextBlock>
        <Button Grid.Row="0" Grid.RowSpan="2" Grid.Column="1" Height="64" Width="64" Command="{Binding NextQuotationCommand}" Style="{StaticResource QuoteButton}" />
    </Grid>
</UserControl>
