<UserControl
    x:Class="Deadfile.Tab.Tab.TabView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:designTime="clr-namespace:Deadfile.Infrastructure.DesignTime;assembly=Deadfile.Infrastructure"
    xmlns:local="clr-namespace:Deadfile.Tab.Tab"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
    d:DataContext="{d:DesignInstance Type=local:TabDesignTimeViewModel,
                                     IsDesignTimeCreatable=True}"
    d:DesignHeight="300"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <UserControl.Resources>
        <designTime:DesignTimeResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Deadfile.Infrastructure;component/Styles/Converters.xaml" />
                <ResourceDictionary Source="pack://application:,,,/Deadfile.Infrastructure;component/Styles/Styles.xaml" />
                <ResourceDictionary Source="pack://application:,,,/Deadfile.Infrastructure;component/Styles/Themables.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Accents/BaseLight.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Accents/Red.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </designTime:DesignTimeResourceDictionary>
    </UserControl.Resources>
    <Grid Background="{StaticResource ContentBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="3" />
            <ColumnDefinition
                Width="Auto"
                MinWidth="100"
                MaxWidth="400" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <GridSplitter
            Grid.Row="1"
            Grid.Column="1"
            Width="3"
            HorizontalAlignment="Stretch"
            Foreground="{DynamicResource GrayBrush1}"
            Visibility="{Binding BrowserAndActionsAreVisible, Converter={StaticResource BooleanToVisibility}}" />
        <ContentControl
            x:Name="NavigationBar"
            Grid.Row="0"
            Grid.Column="0"
            Grid.ColumnSpan="3" />
        <metro:TransitioningContentControl
            x:Name="ContentArea"
            Grid.Row="1"
            Grid.Column="0"
            Grid.ColumnSpan="{Binding BrowserAndActionsAreVisible, Converter={StaticResource BrowserAndActionsAreVisibleToContentColumnSpan}}"
            BorderThickness="0"
            Transition="Default" />
        <ContentControl
            x:Name="BrowserPane"
            Grid.Row="1"
            Grid.Column="2"
            Visibility="{Binding BrowserAndActionsAreVisible, Converter={StaticResource BooleanToVisibility}}" />
        <ContentControl
            x:Name="QuotesBar"
            Grid.Row="2"
            Grid.Column="0"
            Grid.ColumnSpan="{Binding BrowserAndActionsAreVisible, Converter={StaticResource BrowserAndActionsAreVisibleToQuotesColumnSpan}}" />
        <ContentControl
            x:Name="ActionsPad"
            Grid.Row="2"
            Grid.Column="2"
            Visibility="{Binding BrowserAndActionsAreVisible, Converter={StaticResource BooleanToVisibility}}" />
    </Grid>
</UserControl>
