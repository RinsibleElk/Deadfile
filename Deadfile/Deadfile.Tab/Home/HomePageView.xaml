<UserControl x:Class="Deadfile.Tab.Home.HomePageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Deadfile.Tab.Home"
             d:DataContext="{d:DesignInstance Type=local:HomePageDesignTimeViewModel, IsDesignTimeCreatable=True}"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Background="{StaticResource ContentBackgroundBrush}">
        <Button x:Name="Clients">Clients</Button>
        <Button x:Name="Jobs">Jobs</Button>
        <Button x:Name="LocalAuthorities">Local Authorities</Button>
    </StackPanel>
</UserControl>
