<UserControl x:Class="Deadfile.Tab.Reports.UnpaidInvoices.UnpaidInvoicesPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Deadfile.Tab.Reports.UnpaidInvoices"
             xmlns:model="clr-namespace:Deadfile.Model;assembly=Deadfile.Model"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:templates="clr-namespace:Deadfile.Infrastructure.Templates;assembly=Deadfile.Infrastructure"
             xmlns:designTime="clr-namespace:Deadfile.Infrastructure.DesignTime;assembly=Deadfile.Infrastructure"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=local:UnpaidInvoicesPageDesignTimeViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <designTime:DesignTimeResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Deadfile.Infrastructure;component/Styles/Converters.xaml" />
                <ResourceDictionary Source="pack://application:,,,/Deadfile.Infrastructure;component/Styles/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </designTime:DesignTimeResourceDictionary>
    </UserControl.Resources>
    <DockPanel LastChildFill="True">
        <DockPanel DockPanel.Dock="Top" LastChildFill="True" >
            <Button DockPanel.Dock="Right" x:Name="Print">
                Print
            </Button>
            <TextBox controls:TextBoxHelper.Watermark="Filter" Text="{Binding Filter, Mode=TwoWay, Delay=500, UpdateSourceTrigger=PropertyChanged}"/>
        </DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
            <TextBlock Text="Start Date:" Margin="5,0,5,0" VerticalAlignment="Center" />
            <DatePicker SelectedDate="{Binding StartDate, Mode=TwoWay}" Margin="5,0,5,0" />
            <TextBlock Text="End Date:" Margin="5,0,5,0" VerticalAlignment="Center" />
            <DatePicker SelectedDate="{Binding EndDate, Mode=TwoWay}" Margin="5,0,5,0" />
            <CheckBox Margin="5,0,5,0" IsChecked="{Binding IncludeInactive}" VerticalAlignment="Center">
                Include Inactive
            </CheckBox>
        </StackPanel>
        <DataGrid AutoGenerateColumns="False"
                  ItemsSource="{Binding Items}"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeColumns="True"
                  CanUserResizeRows="False"
                  CanUserSortColumns="True"
                  x:Name="Report">
            <DataGrid.Columns>
                <DataGridTemplateColumn IsReadOnly="True" Header="Client" SortMemberPath="ClientName">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate DataType="model:InvoiceModel">
                            <Button Content="{Binding ClientName}"
                                    Command="{Binding Path=DataContext.NavigateToClient, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn IsReadOnly="True" Header="Invoice" SortMemberPath="InvoiceReference">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate DataType="model:InvoiceModel">
                            <Button Content="{Binding InvoiceReference}"
                                    Command="{Binding Path=DataContext.NavigateToInvoice, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn IsReadOnly="True" Header="Project" Binding="{Binding Project}" />
                <DataGridTextColumn IsReadOnly="True" Header="Amount Due" Binding="{Binding GrossAmount}" />
                <DataGridTextColumn IsReadOnly="True" Header="Description" Binding="{Binding Description}" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
