<UserControl x:Class="Deadfile.Tab.Navigation.NavigationBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Deadfile.Tab.Navigation"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=local:NavigationBarDesignTimeViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Orientation="Horizontal" Background="Transparent">
        <Button Height="32" Width="32" Margin="3" x:Name="Home" Style="{StaticResource HomeButton}" />
        <Button Height="32" Width="32" Margin="3" x:Name="Back" Style="{StaticResource BackButton}" />
        <Button Height="32" Width="32" Margin="3" x:Name="Forward" Style="{StaticResource ForwardButton}" />
        <Button Height="32" Width="32" Margin="3" x:Name="Undo" Style="{StaticResource UndoButton}" />
        <Button Height="32" Width="32" Margin="3" x:Name="Redo" Style="{StaticResource RedoButton}" />
    </StackPanel>
</UserControl>
