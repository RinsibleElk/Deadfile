<UserControl  x:Class="Deadfile.Content.Navigation.NavigationBar"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
              xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
              xmlns:navigation="clr-namespace:Deadfile.Content.Navigation"
              xmlns:prism="http://prismlibrary.com/"
              prism:ViewModelLocator.AutoWireViewModel="True"
              mc:Ignorable="d"
              d:DataContext="{d:DesignInstance Type=navigation:NavigationBarDesignTimeViewModel, IsDesignTimeCreatable=True}"
              d:DesignHeight="20" d:DesignWidth="400">
    <StackPanel Orientation="Horizontal">
        <Button Command="{Binding HomeCommand}" Width="100">Home</Button>
        <Button Command="{Binding BackCommand}" Width="100">Back</Button>
        <Button Command="{Binding ForwardCommand}" Width="100">Forward</Button>
    </StackPanel>
</UserControl>
