<UserControl  x:Class="Deadfile.Content.Navigation.NavigationBar"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
              xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
              xmlns:navigation="clr-namespace:Deadfile.Content.Navigation"
              xmlns:prism="http://prismlibrary.com/"
              prism:ViewModelLocator.AutoWireViewModel="True"
              mc:Ignorable="d"
              d:DataContext="{d:DesignInstance Type=navigation:NavigationBarDesignTimeViewModel, IsDesignTimeCreatable=True}"
              d:DesignHeight="50" d:DesignWidth="400"
              Background="Transparent">
    <StackPanel Orientation="Horizontal" Background="Transparent">
        <Button Command="{Binding HomeCommand}" Style="{StaticResource HomeButton}" />
        <Button Command="{Binding BackCommand}" Style="{StaticResource BackButton}" />
        <Button Command="{Binding ForwardCommand}" Style="{StaticResource ForwardButton}" />
    </StackPanel>
</UserControl>
